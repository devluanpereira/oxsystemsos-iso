# OXSystemsOS GRUB theme assets

Current active asset:

- `background.png` (copied from `../../oxs-logo.png`, size `1536x1024`)

The GRUB theme file is `theme.txt` in this same directory.

Menu is centered on lower half of the screen (`top = 69%`) to match the
reference style.
